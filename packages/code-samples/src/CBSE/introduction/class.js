class Square {
  // Constructeur avec définition des attributs
  constructor(height, width) {
    this.height = height;
    this.width = width;
  }

  // Définition d'une méthode
  grow() {
    this.width++;
  }
}
