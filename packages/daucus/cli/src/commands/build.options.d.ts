/**
 * parameters for the "build" command
 *
 * @see {@link BuildCommand}
 */
export interface BuildCommandOptions {
  /**
   * Name of the project to build (by default, all projects will be built)
   */
  project?: string;
}
